interface:
  display_name: 'Backend Controller'
  short_description: 'Criar controllers Nest no backend'
  default_prompt: 'Crie ou revise controllers HTTP do backend no padrão do projeto, com guards/permissões, chamada de use cases e mapeamento correto de erros para status HTTP.'
