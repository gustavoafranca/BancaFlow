{
  "name": "@namespace/workspace",
  "private": true,
  "packageManager": "npm@latest",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "format": "prettier --write ."
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "modules/*"
  ]
}
