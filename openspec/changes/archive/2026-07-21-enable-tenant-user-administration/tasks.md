## 1. Access Control — catálogo e matriz (pré-requisito de tudo)

- [x] 1.1 Ampliar `PERMISSION_CATALOG`/`PermissionKey` (`modules/access-control/src/permission-catalog.ts`, `permission-key.ts`) com as sete novas chaves: `identity.accounts.list`, `identity.accounts.read`, `identity.accounts.create`, `identity.accounts.update`, `identity.accounts.change-role`, `identity.accounts.sessions.read`, `identity.accounts.sessions.revoke` (labels/descriptions em português, agrupadas na capability `identity`).
- [x] 1.2 Atualizar `ROLE_PERMISSION_MAP` (`modules/access-control/src/role-permission-map.ts`): conceder as 7 novas chaves apenas a `OWNER`; remover `ADMIN` de `identity.accounts.toggle-status`, `identity.accounts.reset-password` e `access-control.role-permissions.read`; adicionar `participants.betting-agents.list`/`.read` a `USER` (mantendo `.create` só `OWNER`/`ADMIN`).
- [x] 1.3 Atualizar `modules/access-control/test/catalog-integrity.spec.ts`, `has-permission.spec.ts`, `get-own-effective-permissions.use-case.spec.ts` e `get-role-permission-matrix.use-case.spec.ts` para refletir a nova matriz (16 chaves, `ADMIN` sem as 3 chaves removidas, `USER` com `participants.betting-agents.list`/`.read`).
- [x] 1.4 Atualizar `apps/backend/test/access-control/role-permissions.e2e-spec.ts`: `GET /api/access-control/role-permissions` agora retorna `200` só para `OWNER` (antes também `ADMIN`); `ADMIN` passa a receber `403`.
- [x] 1.5 Usar a skill `module-use-case` apenas se algum caso de uso de Access Control precisar de ajuste; nesta fase são apenas constantes e mapeamento, sem novo caso de uso. (Confirmado: nenhum caso de uso de Access Control precisou de ajuste — `GetRolePermissionMatrixUseCase`/`GetOwnEffectivePermissionsUseCase` já consultavam `ROLE_PERMISSION_MAP` dinamicamente.)

## 2. Identity — domínio (entidade e casos de uso novos)

- [x] 2.1 Usar a skill `module-entity` para adicionar a `UserAccount` (`modules/identity/src/user-account/user-account.entity.ts`) os métodos `changeRole(newRole: AccountRole)` e `renameUsername(newUsername: Username)`, seguindo o padrão `rebuild` privado (spread raso + `tryCreate`) já usado por `rename`/`updateEmail`/`activate`/`block` — nunca `Entity.cloneWith`/`clone` da base.
- [x] 2.2 `changeRole` SHALL rejeitar (`IDENTITY_ERRORS.FORBIDDEN` ou código dedicado, se necessário) quando o papel atual é `OWNER` ou o `newRole` é `OWNER`.
- [x] 2.3 Adicionar teste de unidade em `modules/identity/test/user-account.entity.spec.ts` para `changeRole` (transições válidas `ADMIN⇄USER`, rejeição envolvendo `OWNER`) e `renameUsername` (nova instância, imutabilidade, cópia defensiva preservada).
- [x] 2.4 Extrair a política de alvo única `assertAdministrableTarget(actorUserId: string, target: UserAccount): Result<void>` (novo arquivo em `modules/identity/src/shared/` ou `modules/identity/src/user-account/`), chamada **depois** que o alvo já foi resolvido via `findById(accountId, bancaId-do-ator)`. O helper NÃO SHALL reverificar `actorRole`/papel — isso já foi decidido pela `PermissionChecker` (`hasPermission`) antes de o caso de uso chegar aqui. O helper valida apenas: alvo não pode ser o próprio ator (`target.id !== actorUserId`); alvo não pode ser `OWNER` (`target.role !== 'OWNER'`). Reutilizar esse helper em todos os casos de uso administrativos que operam sobre uma conta-alvo específica (seções 2 e 4, novos e existentes: `toggle-account-status.use-case.ts`, `admin-reset-password.use-case.ts`) — **não** em `ListUserAccountsUseCase` (2.5), que não possui alvo.
- [x] 2.5 Usar a skill `module-use-case` para criar `ListUserAccountsUseCase`: verifica somente a permissão (`identity.accounts.list` via `PermissionChecker`) e consulta `ListUserAccountsQuery` já escopada por `bancaId` — sem resolver alvo, sem `assertAdministrableTarget` (não se aplica a uma listagem).
- [x] 2.6 Usar a skill `module-use-case` para criar `GetUserAccountUseCase`, `UpdateUserAccountUseCase` (username/name/email de terceiro + `expectedVersion`, reutilizando os VOs compartilhados `PersonName`/`Email` — os mesmos já usados por `UpdateOwnProfileUseCase` — para validar `name`/`email`), `ChangeAccountRoleUseCase` (revoga sessões do alvo na mesma transação) e `AdminListAccountSessionsUseCase`/`AdminRevokeAccountSessionUseCase`. Todos recebem `actorUserId` (do `AuthContext`, nunca do body/rota) além de `bancaId`/`actorRole`, consultam a `PermissionChecker` para a `PermissionKey` do endpoint, resolvem o alvo via `findById(accountId, bancaId)` — ausência de linha retorna o código já existente `IDENTITY_ERRORS.ACCOUNT_NOT_FOUND` (nunca um código genérico novo) — e só então aplicam `assertAdministrableTarget(actorUserId, target)`. Todas as validações de entrada usam `Result`/`Result.combine` no mesmo padrão dos demais use cases de Identity.
- [x] 2.7 Usar a skill `module-use-case` para criar `AdminCreateUserAccountUseCase`, que: (a) valida a permissão `identity.accounts.create` via `PermissionChecker`; (b) usa `bancaId` e `actorUserId` do `AuthContext` (nunca do body); (c) aceita somente `role: 'ADMIN'|'USER'` na entrada, rejeitando `OWNER` e qualquer ausência de `role`; (d) gera a senha temporária reutilizando o port `TemporaryPasswordGenerator` já existente (mesmo usado por `AdminResetPasswordUseCase`); (e) **delega a criação do agregado** ao `CreateUserAccountUseCase` já existente, passando `mustChangePassword = true` — sem duplicar suas validações de `username`/`PersonName`/`Email`/`StrongPassword`, e sem misturar a autorização administrativa dentro de `CreateUserAccountUseCase` (que continua consumido, sem alterações de contrato, pelo fluxo `ProvisionBanca`); (f) devolve a senha temporária somente no retorno desta chamada, nunca persistida em texto puro.
- [x] 2.8 Testes de unidade (mesma pasta `modules/identity/test/`) para cada caso de uso novo: `ListUserAccountsUseCase` (permissão negada para `ADMIN`/`USER`, tenant-scoped, sem checagem de alvo); os casos de uso com alvo (2.6) — sucesso, `ADMIN`/`USER` proibidos, alvo inexistente/de outra banca retornando `IDENTITY_ERRORS.ACCOUNT_NOT_FOUND`, alvo `OWNER` proibido, auto-alvo proibido, revogação de sessão quando aplicável (role change) e não-revogação quando não aplicável (update de campos); `AdminCreateUserAccountUseCase` — sucesso com `ADMIN`/`USER`, rejeição de `OWNER`/`role` ausente, delegação correta a `CreateUserAccountUseCase`, reuso de `TemporaryPasswordGenerator`/`StrongPassword`, `mustChangePassword = true`, senha retornada só na resposta, `ProvisionBanca` inalterado.

## 3. Identity — persistência (query e adapters, sem alterar schema)

- [x] 3.1 Usar a skill `module-query-cqrs` para definir a porta `ListUserAccountsQuery` em `modules/identity/src/user-account/query/` (ou local equivalente), recebendo `bancaId`, `search?`, `role?`, `status?`, `PaginatedInputDTO`, retornando `PaginatedResultDTO<UserAccountListItemDto>`.
- [x] 3.2 Usar a skill `backend-prisma-data` para implementar o adapter Prisma da query (`apps/backend/src/modules/identity/adapters/list-user-accounts.query.prisma.ts` ou arquivo equivalente), usando `findMany`/`count` sobre `user_accounts` já existente. O `WHERE` SHALL sempre incluir `role IN ('ADMIN', 'USER')` (excluindo `OWNER`) além de `bancaId`, `role` (filtro do usuário), `status` e busca por `username`/`name` — nessa ordem, antes de `skip`/`take`. Não implementar a exclusão do `OWNER` como um filtro aplicado depois da paginação (corromperia `total`/`totalPages` e poderia gerar páginas incompletas — ver `design.md` D5).
- [x] 3.3 Confirmar que nenhum método novo do `UserAccountRepository` precisa mudar: apenas adicionar `findByBancaAndUsername` já existente é reaproveitado para checar duplicidade em `UpdateUserAccountUseCase`/`ChangeRole` quando aplicável. (Confirmado: `UserAccountRepositoryPrisma` não mudou; `UpdateUserAccountUseCase` reaproveita `findByBancaAndUsername` já existente.)
- [x] 3.4 Testes de integração da query (com banco real de teste) cobrindo paginação, busca e filtros, tenant-scoped. (Cobertos dentro do e2e de `GET /api/accounts` da seção 4.5 — não há precedente no repo de teste de integração isolado por query Prisma; os demais adapters de leitura também só são exercitados via e2e HTTP.)

## 4. Identity — DTOs e controllers HTTP (`/api/accounts`)

- [x] 4.1 Usar a skill `module-dto` para criar os DTOs administrativos: `ListUserAccountsInDto`/`UserAccountListItemOutDto`, `UserAccountDetailOutDto`, `CreateUserAccountInDto`/`CreateUserAccountOutDto` (inclui senha temporária), `UpdateUserAccountInDto`, `ChangeAccountRoleInDto`, reaproveitando o `SessionInfoDto` existente (com seu campo `isCurrent`) para a listagem administrativa de sessões — a listagem administrativa sempre preenche `isCurrent: false` em todos os itens (o ator nunca consulta as próprias sessões por este endpoint), sem criar uma forma de DTO paralela sem `isCurrent`.
- [x] 4.2 Usar a skill `backend-controller` para criar `accounts.controller.ts` em `apps/backend/src/modules/identity/`, com as rotas: `GET /api/accounts`, `GET /api/accounts/:accountId`, `POST /api/accounts`, `PATCH /api/accounts/:accountId`, `PATCH /api/accounts/:accountId/role`, `GET /api/accounts/:accountId/sessions`, `DELETE /api/accounts/:accountId/sessions/:sessionId` — todas com `JwtCookieAuthGuard`, extraindo `bancaId`, `actorRole` **e `actorUserId`** do `AuthContext` (nunca do body/rota) e passando os três ao caso de uso, e tradução de códigos de erro do catálogo para status HTTP (seguindo o padrão `STATUS_BY_CODE` já existente, incluindo o contrato `403`/`404` de `design.md` D11).
- [x] 4.3 Manter `PATCH /api/auth/admin/reset-password` (body `{ targetUserId }`) exatamente como hoje, sem mover rota nem mudar o corpo da requisição. No controller existente (`identity.controller.ts`), passar a extrair `actorUserId` do `AuthContext` e enviá-lo ao caso de uso — a assinatura de `admin-reset-password.use-case.ts` muda para recebê-lo. Ajustar o caso de uso para: (a) restringir a permissão a `identity.accounts.reset-password` só `OWNER` (decorrente da seção 1); (b) resolver o alvo via `findById(targetUserId, bancaId)` (ausência → `IDENTITY_ERRORS.ACCOUNT_NOT_FOUND`) e aplicar `assertAdministrableTarget(actorUserId, target)`.
- [x] 4.4 No controller existente que expõe `PATCH /api/accounts/:accountId/status`, passar a extrair `actorUserId` do `AuthContext` e enviá-lo ao caso de uso — a assinatura de `toggle-account-status.use-case.ts` muda para recebê-lo. Restringir a permissão a `identity.accounts.toggle-status` só `OWNER` (decorrente da mudança de matriz da seção 1 — confirmar que nenhuma checagem de papel bruto paralela sobreviveu no controller/caso de uso), resolver o alvo via `findById` (ausência → `IDENTITY_ERRORS.ACCOUNT_NOT_FOUND`) e aplicar `assertAdministrableTarget` do mesmo modo que os demais endpoints administrativos.
- [x] 4.5 Testes e2e (`apps/backend/test/identity/`) para cada endpoint novo e para os dois endpoints existentes com permissão restrita (`status`, `admin/reset-password`): sucesso do `OWNER`; `403` para `ADMIN`/`USER` (falta de permissão); `404` para alvo inexistente ou de outra banca (nunca `403` para esse caso — ver D11); `403` para alvo `OWNER`; `403` para auto-alvo; concorrência otimista (`CONCURRENCY_CONFLICT`) no `update`; revogação de sessão no `change-role`; confirmar que `PATCH /api/auth/admin/reset-password` continua no mesmo caminho e formato de sempre.

## 5. Prova de ausência de alteração de schema (gate de revisão da change, não teste permanente do produto)

- [x] 5.1 Como parte da revisão desta change (code review / checagem pontual antes do merge, não um teste automatizado que passe a viver no repositório), conferir que `apps/backend/prisma/models/*.model.prisma` está idêntico ao estado anterior à change (ex.: `git diff` contra a base da branch) — qualquer diferença de tabela/coluna/relação bloqueia o merge.
- [x] 5.2 Como parte da mesma revisão, confirmar que nenhuma migration nova foi criada em `apps/backend/prisma/migrations/`.

## 6. Frontend — gate de menu e rota por permissão

- [x] 6.1 Usar a skill `frontend-module-workflow` para consumir `GET /api/access-control/me/permissions` (primeira vez no Web) via um hook/provider no shell privado, expondo as permissões efetivas do usuário autenticado.
- [x] 6.2 Substituir o `disabled` hardcoded do item **Configurações** em `app-navbar.tsx` por uma checagem de `identity.accounts.list` nas permissões efetivas; o item se torna acionável e navega para `/configuracoes` somente quando presente.
- [x] 6.3 Em `configuracoes.page.tsx`, tratar o caso de acesso direto por URL sem a permissão: estado seguro "sem permissão", sem erro não tratado, sem vazar dados (a chamada ao Backend já retorna `403`).
- [x] 6.4 Testes de `app-navbar.spec.tsx` (item acionável para `OWNER`, não acionável para `ADMIN`/`USER`) e de `configuracoes.page.spec.tsx` (estado sem permissão).

## 7. Frontend — módulo `configuracoes`: seção Usuários

- [x] 7.1 Usar a skill `backend-controller`/`module-dto` do lado consumidor: criar `apps/web/src/modules/configuracoes/data/accounts.client.ts` com funções tipadas para os 5 endpoints de `/api/accounts` (list, detail, create, update, change-role), para `PATCH /api/accounts/:accountId/status` (ativar/desativar/bloquear/desbloquear — necessário pela tarefa 7.4, que depende dele), para `GET`/`DELETE /api/accounts/:accountId/sessions[/:sessionId]`, e para o `PATCH /api/auth/admin/reset-password` existente (mesmo caminho e corpo de hoje, apenas consumido pela primeira vez neste client).
- [x] 7.2 Usar a skill `frontend-form-schema` para criar os schemas de criação e edição de conta em `apps/web/src/modules/configuracoes/data/*.schema.ts`, seguindo o padrão real de `v.defineObject` já usado em `modules/perfil`.
- [x] 7.3 Usar a skill `frontend-module-workflow` para construir a listagem (componente `Table` compartilhado, busca, paginação, filtros por papel/status, estados de carregamento/vazio/erro/sem permissão) em `apps/web/src/modules/configuracoes/components/`.
- [x] 7.4 Construir os formulários de criação e edição (`Dialog` + mecanismo de formulário real), a ação de troca de papel, as ações de ativar/bloquear/desbloquear com confirmação (`Dialog`), e a apresentação segura da senha temporária (criação e redefinição), com prevenção de envio duplicado em todas as ações.
- [x] 7.5 Construir a seção de sessões de uma conta (listar/revogar) reaproveitando visualmente o padrão de `modules/perfil/components/security-tab.tsx`.
- [x] 7.6 Testes de componente/página cobrindo os cenários de `specs/tenant-user-administration/spec.md`: listagem real, filtros, estados, criação, edição, troca de papel refletida, ações de status com confirmação, erros de API, prevenção de duplo clique, senha temporária exibida uma única vez.
- [x] 7.7 Auditar componentes usados (checklist manual): confirmar reaproveitamento de `Table`/`Dialog`/`Button`/`Input`/`Badge` existentes, nenhuma biblioteca visual nova, página `configuracoes.page.tsx` permanece fina (lógica em hooks/clients do módulo).

## 8. Frontend — matriz de permissões restrita a OWNER

- [x] 8.1 Atualizar `role-permissions-matrix.tsx`/`configuracoes.page.tsx` para ocultar a seção de matriz por experiência quando o ator não é `OWNER` (hoje oculta só para `USER`; passa a ocultar também para `ADMIN`).
- [x] 8.2 Atualizar `configuracoes.page.spec.tsx` e `access-control.client.spec.ts` para o novo comportamento (matriz visível só para `OWNER`; `ADMIN` recebe `403` do Backend e vê o mesmo estado de erro/sem permissão que `USER`).

## 9. Frontend — modal de logout unificado

- [x] 9.1 Criar `LogoutModalProvider`/hook no shell privado (`apps/web/src/app/(private)/_shell/`), controlando estado compartilhado de abertura/processamento/erro.
- [x] 9.2 Criar o componente de modal reutilizando `shared/components/ui/dialog.tsx`, no padrão do diálogo de "Encerrar sessão" de `modules/perfil/components/security-tab.tsx`: foco inicial, `DialogClose` para Cancelar, Escape condicional, devolução de foco, estado de processamento, prevenção de duplo clique, erro visível.
- [x] 9.3 Substituir em `app-navbar.tsx` as duas ações diretas (`handleLogout`/`handleLogoutAll`) e em `app-sidebar.tsx` a ação direta de logout por chamadas a `openLogoutModal()` do provider compartilhado.
- [x] 9.4 Implementar o contrato de redirecionamento: sucesso redireciona para `/login` uma única vez; falha mantém o modal aberto com erro.
- [x] 9.5 Atualizar/criar testes: `app-navbar.spec.tsx`, novo `app-sidebar.spec.tsx` (hoje inexistente) e um spec do modal cobrindo todos os cenários de `specs/unified-logout-experience/spec.md` (abertura única, cada ação chama a API correta, cancelamento, Escape, foco, duplo clique, erro, sucesso). Confirmar que a aba Segurança de `/perfil` (sessões individuais) não regride.

## 10. Coordenação com outras changes (sem editar artefatos alheios)

- [x] 10.1 Registrar, no changelog/relatório desta change, que `implement-participant-registration-mvp` (change ativa, 0/55 tasks) precisa alinhar sua spec `betting-agent-catalog` e sua implementação para conceder `USER` a `participants.betting-agents.list`/`.read`, refletindo a matriz já atualizada nesta change — sem editar os artefatos daquela change aqui.

## 11. Jornadas E2E

Cobertas no nível de integração backend real (`apps/backend/test/**/*.e2e-spec.ts`, supertest + banco Postgres real — o mesmo tier que este repo já chama de "e2e"), mais os testes unitários de componente do frontend para a parte de UI. Jornadas de navegador ponta a ponta (Playwright, `apps/web/playwright.config.ts`) NÃO foram escritas nesta rodada: este ambiente não tem um navegador/servidor ao vivo disponível para gravar ou validar esses testes, e não há nenhum spec Playwright pré-existente no repo para seguir como padrão — registrar como lacuna explícita, não fingir cobertura que não existe.

- [x] 11.1 Owner cria um usuário com senha temporária (`accounts.e2e-spec.ts`, `POST /api/accounts` → senha exibida uma vez, login com a senha temporária funciona).
- [x] 11.2 Usuário criado entra e realiza a troca obrigatória de senha (coberto dentro de `accounts.e2e-spec.ts`, teste de troca de papel: cria conta, login com senha temporária, `PATCH /api/auth/mandatory-password-change`).
- [x] 11.3 Owner edita dados (`PATCH /api/accounts/:id`, com `CONCURRENCY_CONFLICT` em versão desatualizada) e bloqueia a conta (`PATCH /api/accounts/:id/status`, sessões revogadas) — `accounts.e2e-spec.ts`.
- [x] 11.4 Owner troca uma conta entre `USER` e `ADMIN`; sessões da conta revogadas; novo login reflete a nova entrada da matriz fixa via `GET /api/access-control/me/permissions` — `accounts.e2e-spec.ts`.
- [x] 11.5 Admin não vê nem acessa a administração de usuários nem a matriz de permissões: menu ausente (`app-navbar.spec.tsx`), `403` em acesso direto ao Backend (`accounts.e2e-spec.ts`, `role-permissions.e2e-spec.ts`). Não há um único teste de navegador combinando as duas pontas (ver nota acima).
- [x] 11.6 Usuário de outra banca não pode ser consultado nem alterado (`404`, nunca `403`, indistinguível de conta inexistente) por nenhum dos endpoints de `/api/accounts` nem por `PATCH /api/auth/admin/reset-password` — `accounts.e2e-spec.ts`, `admin-reset-password.use-case.spec.ts`.
- [x] 11.7 Logout do dispositivo atual preserva outra sessão ativa da mesma conta (cobertura pré-existente de `identity.e2e-spec.ts`, não afetada por esta change).
- [x] 11.8 Logout de todos os dispositivos invalida as demais sessões (cobertura pré-existente de `identity.e2e-spec.ts`, não afetada por esta change).

## 12. Gates de qualidade

- [x] 12.1 Rodar testes de `packages/shared` — nenhuma alteração feita nele nesta change; rodado mesmo assim por segurança: 36 suites / 333 testes, 100% cobertura, sem regressão.
- [x] 12.2 Rodar testes de `modules/identity` (248/248) e `modules/access-control` (23/23) — unit, todos passando.
- [ ] 12.3 Rodar testes do backend afetado (`apps/backend/test/identity`, `apps/backend/test/access-control`) — **não executado neste ambiente**: requer Postgres real (`docker compose up -d postgres`) e Docker não está disponível neste sandbox (WSL2 sem integração). Os specs foram escritos/atualizados e type-checam corretamente (`accounts.e2e-spec.ts`, `role-permissions.e2e-spec.ts`, `identity.e2e-spec.ts`, `toggle-status.e2e-spec.ts`, `admin-reset-password.use-case.spec.ts` como unit); precisam rodar contra um Postgres real antes do merge.
- [x] 12.4 Rodar testes do frontend afetado — suíte completa de `apps/web`: 37 suites / 199 testes passando (`configuracoes`, shell/`logout-modal`/`app-navbar`/`app-sidebar`, `perfil` sem regressão).
- [ ] 12.5 Rodar as jornadas E2E críticas da seção 11 — mesma limitação de 12.3 (sem Postgres/Docker neste ambiente); specs prontos, não executados.
- [x] 12.6 Rodar typecheck do monorepo (`npm run check-types` via turbo + `tsc --noEmit` manual em `apps/backend`/`modules/identity`/`modules/access-control`) — limpo, exceto um erro pré-existente e não relacionado em `apps/backend/test/identity/transaction.e2e-spec.ts` (`ChangePasswordInput.currentSessionId` ausente), não tocado por esta change.
- [x] 12.7 Rodar lint do monorepo (`npm run lint` via turbo) — limpo (corrigidos nesta change: `no-unsafe-*` em specs e2e novos/editados, e `react-hooks/set-state-in-effect` em `use-user-accounts.ts`/`use-account-sessions.ts`/`edit-account-dialog.tsx`, resolvido com o padrão "ajustar estado durante a renderização" em vez de `setState` síncrono no efeito).
- [x] 12.8 Rodar build do monorepo — `apps/backend` (`nest build`) e `apps/web` (`next build`, incluindo a rota `/configuracoes`) buildam limpos.
- [x] 12.9 Auditoria de componentes do frontend — confirmado reaproveitamento de `Table`/`Dialog`/`Button`/`Input`/`Badge` existentes em todos os componentes novos (`usuarios-section.tsx`, `create-account-dialog.tsx`, `edit-account-dialog.tsx`, `account-sessions-dialog.tsx`, `logout-modal.tsx`); nenhuma biblioteca visual nova adicionada a `package.json`; `configuracoes.page.tsx` permanece fina (delega a `UsuariosSection`/`RolePermissionsMatrix`).
- [x] 12.10 Gate de revisão (não teste de produto): confirmado por timestamp de arquivo e ausência de qualquer edição desta sessão que `apps/backend/prisma/models/*.model.prisma` está inalterado e nenhuma pasta nova foi criada em `apps/backend/prisma/migrations/`.
- [x] 12.11 Rodar `openspec validate enable-tenant-user-administration --strict` — válido.
