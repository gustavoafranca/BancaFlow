## 1. Baseline e auditoria inicial

- [x] 1.1 Medir e registrar o baseline atual da suíte Web (`npm run test` em `apps/web`): número de testes, suítes e tempo total (referência atual: 236 testes, 42 suítes, ~13s), para comparação ao final da change.
- [x] 1.2 Auditar em `apps/web/src/**` todas as implementações atuais de drawer (`pessoas.page.tsx`, `user-account-drawer.tsx`, `AcertoDrawer.tsx`, `DetailDrawer.tsx`) e registrar, para cada uma, quais campos/abas/ações de domínio precisam ser preservados na migração e quais são telas mock sem persistência real (Pessoas, Acerto).

## 2. Drawer compartilhado (fundação)

- [x] 2.1 Generalizar `apps/web/src/shared/components/ui/dialog.tsx` (variante `drawer`) ou extrair um `Drawer` compositor equivalente, movendo a largura para `style` inline controlável (a classe atual `max-w-[460px]` é estática e não suporta resize): adicionar `minWidth`/`maxWidth`, redimensionamento por arraste de mouse, redimensionamento por teclado, maximizar/restaurar. **Feito**: novo `apps/web/src/shared/components/ui/drawer.tsx` (compositor sobre os primitives Radix já usados por `Dialog`, não um `variant` de `DialogContent`).
- [x] 2.2 Adicionar suporte a `mode: 'create' | 'edit' | 'view'` com rodapé fixo por modo (Fechar+Salvar; Excluir+Fechar+Salvar Alterações; Fechar+Editar), ocultando ações sem permissão em vez de desabilitá-las. **Feito**: `DrawerFooter`.
- [x] 2.3 Adicionar suporte a estado `loading` que desabilita as ações do rodapé sem fechar o drawer nem perder conteúdo preenchido. **Feito**: prop `loading` em `DrawerFooter`.
- [x] 2.4 Garantir que o corpo do drawer tenha scroll independente (`DialogBody` já existente) e composição com o primitive `Tabs` compartilhado para casos de múltiplas abas. **Feito**: `DrawerBody` reexporta `DialogBody`; `Tabs` compõe livremente dentro do corpo.
- [x] 2.5 Confirmar que o overlay herda os tokens do tema ativo (claro/escuro) via o mesmo mecanismo já usado por `Dialog`/`Select`, sem cor hardcoded por instância. Definir e aplicar de forma consistente o offset de topo do drawer (colar no topo sobre a navbar, como `user-account-drawer` hoje, vs. abaixo da navbar como Pessoas/Acerto — ver pergunta em aberto do design). **Feito**: classes `bg-popover`/`text-popover-foreground`/`border-border` (mesmos tokens do Dialog); offset `top-[54px] h-[calc(100vh-54px)]` (abaixo da navbar, decisão registrada no design).
- [x] 2.6 Criar/expandir `dialog.spec.tsx` (ou novo `drawer.spec.tsx`) cobrindo: abrir/fechar, foco inicial e retorno de foco, Escape, resize por mouse e teclado dentro dos limites, maximizar/restaurar, rodapé por modo e por permissão, loading, e herança de tema claro/escuro. **Feito**: `drawer.spec.tsx`, 13 testes passando.

## 3. Migração: Configurações → Usuários

- [x] 3.1 Migrar `apps/web/src/modules/configuracoes/components/user-account-drawer.tsx` para o Drawer generalizado (grupo 2), preservando abas/ações reais (dados, papel, status, sessões, senha temporária), o comportamento de `use-account-sessions.ts` e os modais de confirmação existentes. **Nota de escopo**: o drawer de detalhe não tinha modo `view` separado antes (abria já editável); para atender ao contrato do Drawer canônico (`view`→`Editar`→`edit`), foi adicionado o toggle visualização/edição — Dados vira somente-leitura em `view`; Papel/Status/Senha/Sessões continuam sempre visíveis/acionáveis (ações independentes, não fazem parte do formulário salvo por "Salvar Alterações"). `Excluir` omitido (sem endpoint).
- [x] 3.2 Atualizar `usuarios-section.tsx`/`usuarios-section.spec.tsx`: remover asserções de comportamento de drawer já cobertas pelo teste do componente compartilhado (2.6 — removido o teste de Escape, duplicado), mantendo apenas testes de integração de domínio (dados, paginação, filtros, ações de conta, novo teste do toggle visualização/edição).
- [ ] 3.3 Validar manualmente em claro/escuro/desktop/mobile que o drawer de Usuários com resize/maximizar não regride em relação ao comportamento e dados atuais.

## 4. Migração: Pessoas e Vínculos

- [x] 4.1 Migrar `apps/web/src/modules/pessoas/pages/pessoas.page.tsx` para consumir o Drawer compartilhado, usando o visual atual (header, resize, maximizar, abas Dados/Vínculos, rodapé por modo) como referência de paridade, sem portar o markup inline como está. Registrar que Pessoas permanece tela mock (sem persistência real) — a migração é de paridade visual/estrutural, não adiciona contrato de dados. **Nota**: `Tabs` compartilhado (Dados/Vínculos) substitui a barra de abas custom (visual de segmented control, não a sublinha bespoke — um único padrão de Tabs no app). `Salvar`/`Salvar Alterações` fecha o drawer (mock sem persistência, sem contrato novo). Como consequência, `variant="drawer"` de `dialog.tsx` ficou sem consumidores e foi removido (limpeza, sem órfão) — `Dialog` volta a ser só modal centralizado; `DrawerContent` que suporta resize/maximizar.
- [x] 4.2 Atualizar `pessoas.page.spec.tsx` para cobrir apenas a integração de domínio de Pessoas (tipos, percentual de participação, vínculo com cambistas), removendo qualquer asserção duplicada de comportamento genérico de drawer.

## 5. Padronização de status (Pessoas, Cambistas, Turnos)

- [x] 5.1 Padronizar a apresentação de status como `Ativo`/`Inativo` em Pessoas e demais cadastros sem estado de bloqueio autoritativo no backend, reusando os mesmos componentes visuais de badge já usados em Contas de Usuário. **Feito**: tabela de Pessoas usa `Badge variant="success"|"danger"` (Cambistas/Turnos ainda não migrados para o Drawer nesta change — sem tela própria a ajustar aqui).
- [x] 5.2 Confirmar e documentar (comentário/README do módulo ou nota em `account-status.presentation.ts`) que Contas de Usuário é exceção deliberada, mantendo os 3 estados (`ACTIVE`/`INACTIVE`/`BLOCKED`) já autoritativos no backend, sem alteração de schema/migration.
- [x] 5.3 Onde bloqueio existir fora de Contas de Usuário, expor como ação independente (`Bloquear`/`Desbloquear`) e não como um terceiro valor de status na UI. Nenhum outro domínio nesta change tem bloqueio; regra documentada para quando existir.

## 6. Área de Configurações: sidebar por rota

- [x] 6.1 Criar layout de área para `apps/web/src/app/(private)/configuracoes` com sidebar interna fixa e sub-rotas (`/configuracoes/usuarios`, `/configuracoes/perfis`, e placeholders para as demais seções conforme decisão da pergunta em aberto 1 do design). **Decisão aplicada**: seções sem capability real (Turnos/Jogo/Segurança/Auditoria) **omitidas** da sidebar (resolvida com o usuário) — sem placeholders.
- [x] 6.2 Mover o conteúdo de `configuracoes.page.tsx` (Usuários, Perfis de acesso) para as sub-rotas correspondentes, preservando o gate por `identity.accounts.list` e os estados `loading`/`forbidden`/`error`. Remover a navegação por `Tabs` de topo entre seções. **Feito**: `configuracoes.page.tsx` removido; substituído por `configuracoes-layout.tsx` (gate+sidebar) + `usuarios.page.tsx` + `perfis.page.tsx` (páginas finas). O bloco fixo "Demais configurações ainda não estão disponíveis" foi removido junto — como as seções não implementadas nem aparecem mais na sidebar, não há mais navegação enganosa a desmentir ali.
- [x] 6.3 Fazer `/configuracoes` redirecionar para a primeira seção acessível ao usuário e **manter** o link do menu de conta apontando para `/configuracoes` (não quebrar `app-navbar.tsx`); atualizar `app-navbar.spec.tsx` apenas se o destino do redirect exigir, preservando a asserção de que o item existe/some por permissão. **Feito**: `redirect('/configuracoes/usuarios')` (client-side, permitido durante render); `app-navbar.tsx`/spec inalterados (só checam `href="/configuracoes"`).
- [x] 6.4 Implementar destaque do item ativo na sidebar conforme a rota atual, e ocultar itens cuja `PermissionKey` o usuário não possui, protegendo a rota mesmo em acesso direto por URL. **Nota**: Usuários e Perfis de acesso compartilham hoje a mesma `PermissionKey` (`identity.accounts.list`) — não há ainda um caso real de "um item visível, outro oculto"; a sidebar já está estruturada por item para quando isso divergir.
- [x] 6.5 Para seções ainda não implementadas (Turnos, Configuração do Jogo, Segurança, Auditoria), reusar o estado honesto de "capability indisponível" já existente em `configuracoes.page.tsx`, ou omitir o item da sidebar até a capability existir — nunca uma página placeholder que finge estar pronta. **Feito**: omitidas (ver 6.1).
- [x] 6.6 Atualizar `configuracoes.page.spec.tsx` (e specs novos por sub-rota) para cobrir: navegação por rota, destaque de item ativo, ocultação por permissão, proteção de rota direta e o estado de capability indisponível. **Feito**: `configuracoes.page.spec.tsx` removido (componente que testava não existe mais); novo `configuracoes-layout.spec.tsx` cobre gate (loading/forbidden/owner), destaque por rota (`usePathname` mockado) e ausência de itens de capability inexistente. `usuarios-section.spec.tsx`/`role-permissions-matrix.spec.tsx` continuam cobrindo o conteúdo de domínio de cada sub-rota, sem duplicação.
- [x] 6.7 Atualizar o E2E `apps/web/e2e/logout-and-user-administration.e2e.spec.ts` (linhas ~107-108): a asserção `toHaveURL(/\/configuracoes$/)` passa a esperar a sub-rota de Usuários, e `getByRole('tab', { name: 'Usuários' })` passa a validar o item de navegação (link/nav) da sidebar em vez de uma aba. **Feito**, incluindo o label do botão de criação (`Criar conta` → `Salvar`).

## 7. Migração incremental restante (fase 2 — avaliar risco antes de iniciar)

- [x] 7.1 Registrar que `AcertoDrawer.tsx` (painel de ação "confirmar acerto") e `DetailDrawer.tsx` (detalhe somente leitura) são de um módulo mock e **não** seguem o padrão CRUD criar/editar/visualizar de recurso; avaliar se a migração para o Drawer compartilhado agrega valor agora ou fica como fase 2 explícita, sem bloquear os grupos anteriores. **Decisão (resolvida com o usuário)**: fase 2, fora desta change — nenhum código de Acerto foi tocado.
- [ ] 7.2 Se migrado nesta change: migrar `AcertoDrawer.tsx`/`DetailDrawer.tsx` preservando toda regra específica de Acerto, e atualizar `acerto.page.spec.tsx` removendo duplicação de testes de drawer. **Não aplicável nesta change** (ver 7.1) — mantido como item de fase 2 futura.

## 8. Gates finais e fechamento OpenSpec

- [x] 8.1 Rodar `npm run lint`, `npm run check-types`, `npm run test` e `npm run build` no Web e confirmar que passam sem afrouxar asserções existentes. **Lint**: 2 problemas reais introduzidos (variáveis não usadas em `pessoas.page.tsx`, interface vazia em `dialog.tsx`) corrigidos; resta 1 warning pré-existente e não relacionado (`watch()` do React Hook Form em `user-account-drawer.tsx`, já existia antes desta change). **Types**: `npm run check-types` limpo. **Test**: 251/251 passando (ver 8.2). **Build**: ver nota abaixo.
- [x] 8.2 Medir o tempo total da suíte Web novamente e confirmar que é igual ou menor que o baseline registrado na task 1.1. **Resultado**: 251 testes / 43 suítes (era 236/42), ~12,7-16s de wall-clock em 3 execuções (era 13,2-16,9s medido em execuções anteriores no mesmo ambiente — a variância observada em runs idênticos, sem mudança de código, é maior que a diferença entre baseline e agora). Sem regressão clara; sem ganho líquido de tempo ainda, porque a consolidação completa (remover os drawers de Acerto) ficou para a fase 2 — o novo `drawer.spec.tsx` adiciona cobertura necessária ao componente compartilhado que antes não existia isoladamente.
- [ ] 8.3 Fazer validação visual manual em claro/escuro, desktop/mobile, com o Drawer aberto em pelo menos dois domínios migrados (Usuários e Pessoas), registrando contraste, foco visível e ausência de popup claro no dark mode. **Parcial — limitação de ambiente**: screenshot/validação visual real de navegador não foi possível neste sandbox (Chromium headless falha por biblioteca de sistema ausente — `libnspr4.so` — e não há acesso root/sudo para instalar; `npx playwright install-deps` falhou por falta de sudo interativo). Validado o que dava para verificar sem navegador contra o stack real (Postgres + backend Nest + Next dev já rodando, não mocks): login real via API, troca obrigatória de senha real, e busca do HTML server-renderizado com cookie de sessão real — `/pessoas` renderiza a tabela e "Adicionar Pessoa" sem erro; `/configuracoes/usuarios` monta o componente correto (`ConfiguracoesLayout`, confirmado pelo texto "Carregando Configurações..." no HTML) sem vazar o estado "sem permissão"; nenhum erro 500/crash em nenhuma rota tocada. **Não verificado de fato**: aparência visual (contraste, foco, popup no dark mode) e interação (resize/maximizar/clique), pois dependem de JS executado em navegador real, que este ambiente não consegue rodar. Recomendo ao usuário rodar `/run-skill-generator` ou validar manualmente em um ambiente com navegador antes do merge.
- [x] 8.4 Confirmar por `git diff` que nenhum arquivo de backend, Prisma model/migration ou permissão foi criado ou alterado. **Confirmado**: os únicos arquivos de `apps/backend`/`prisma`/`modules/access-control`/`modules/identity` com diferença no working tree já existiam modificados/untracked antes do início desta change (trabalho prévio do usuário, não relacionado); nenhum foi tocado nesta sessão.
- [x] 8.5 Rodar `openspec validate standardize-frontend-drawer-and-settings-nav --strict` e corrigir inconsistências antes de considerar a implementação concluída. **Válida.**
- [ ] 8.6 Sincronizar as specs desta change para `openspec/specs/` e arquivá-la (`openspec-archive-change`). Esta change é autossuficiente e não depende do arquivamento de `enable-`/`refine-`. **Aguardando confirmação do usuário antes de arquivar** (ação que mexe em `openspec/specs/` principal).

## 9. Registro: reconciliação separada de `enable-`/`refine-` (fora desta change)

- [ ] 9.1 Registrar como tarefa dedicada (não bloqueante desta change) a reconciliação dos deltas defasados de `enable-tenant-user-administration` antes de arquivá-la: 8 requisitos em 5 capabilities que dropam/renomeiam cenários, incluindo cenários legítimos de segurança (`user-account-management`: "Cross-banca management is rejected", "Admin unblock resets the lockout counter"; `credential-management`: reset de senha administrativo). Preservar cada cenário legítimo e converter renames para `RENAMED Requirements`.
- [ ] 9.2 Ao reconciliar `refine-tenant-user-administration-experience`, remover/ajustar o requisito "Settings is organized into high-level tabs for OWNER", pois esta change o superou com `settings-area-navigation` (sidebar por rota) — para o main não terminar com "abas" e "sidebar por rota" simultaneamente.

## 10. Selection Button Group (ajuste solicitado antes do archive)

- [x] 10.1 Adicionar `@radix-ui/react-radio-group` (mesma família Radix já usada por `Dialog`/`Select`/`Tabs`) e criar `apps/web/src/shared/components/ui/selection-button-group.tsx`, reaproveitando os valores de cor de `badgeVariants` (`badge.tsx`) por variante — sem paleta nova. Testes (`selection-button-group.spec.tsx`): semântica radiogroup/radio, seleção única, navegação por teclado, `disabled` no grupo e por opção individual.
- [x] 10.2 Adicionar `STATUS_SELECTION_OPTIONS`/`actionForTargetStatus` a `account-status.presentation.ts`, com teste próprio (`account-status.presentation.spec.ts`): mapeia a opção de Status selecionada (Ativo/Inativo/Bloqueado — as 3 reais, resolvido com o usuário) para a ação correta (`activate`/`deactivate`/`block`/`unblock`), preservando os efeitos de domínio existentes (revogação de sessão em `block`/`deactivate`).
- [x] 10.3 Migrar `user-account-drawer.tsx`: seção "Papel" (`Tornar ADMIN`) e "Status" (`Ativar`/`Desativar`/`Bloquear`) viram `SelectionButtonGroup`; selecionar opção diferente da atual abre o mesmo modal de confirmação já existente (não muda direto). `usuarios-section.spec.tsx` atualizado (2 testes reescritos para `radiogroup`/`radio`).
- [x] 10.4 Migrar `pessoas.page.tsx`: Tipo e Status (view/edit) passam a usar o mesmo `SelectionButtonGroup` — incluindo tornar o Status realmente funcional (antes era decorativo, sempre "Ativo" fixo, sem `onClick`). `pessoas.page.spec.tsx` com novo teste cobrindo `radiogroup`/disabled em visualização/seleção em edição.
- [x] 10.5 Rodar `lint`/`check-types`/`build`/suíte completa novamente: 263/263 testes, 45 suítes, sem erro/warning novo (só o warning pré-existente de `watch()` do React Hook Form).
