## 1. Auditoria e inventário (sem movimentações)

- [x] 1.1 Ler integralmente `README.md`, `apps/web/README.md`, `apps/web/AGENTS.md`, `package.json`, `tsconfig.json`, `next.config.ts`, config Jest e Tailwind/PostCSS, e os guias relevantes em `node_modules/next/dist/docs/`
- [x] 1.2 Gerar o inventário verificável de todo `apps/web/src/**` e assets referenciados em `public/**`: caminho, exports, imports, consumidores, responsabilidade, boundary server/client + justificativa, dependências externas, estado e classificação proposta
  - **Reaberta após revisão (P2), atualizada**: [.docs/reports/web-frontend-inventory.md](../../../.docs/reports/web-frontend-inventory.md) estava desatualizado (snapshot pré-migração) — atualizado em uma passada para refletir o estado atual: os 9 `modules/<domain>`, `/unavailable`, `useCurrentUser`, `tenant-context`, `e2e/` (Playwright), `Button`/`Input` reclassificados de órfão para usados, duplicação bespoke reclassificada como exceção aceita (D9) em vez de achado novo.
  - **Novos achados registrados pela atualização** (não corrigidos nesta sessão — candidatos a limpeza futura, Fase 9 ou change dedicada): 3 definições incompatíveis de `Turno` (`shared`, `lancamentos`, `premios` — a última com valores capitalizados diferentes); `Dialog` compartilhado sem nenhum consumidor real; `modules/identity` ainda um placeholder desconectado; `refresh`/`changePassword` em `auth.client.ts` continuam órfãos; `void toSession(claims)` ainda descartado no server (o bug de identidade hardcoded foi corrigido por um caminho client-side diferente, `useCurrentUser`, não por ligar esse `Session` descartado); 7 `logo.png` por módulo e 12 `illustration-*.svg` não usados sob `public/design-imports/`.
- [x] 1.3 Registrar duplicações e smells: `ThemeToggle` (3 ocorrências), conjuntos de ícones (4 lugares), helpers (`fmt`/`initials`/turnos), primitives recriadas por rota, `use client` desnecessário, imports profundos, componentes gigantes
- [x] 1.4 Registrar órfãos: `shared/components/ui/{button,input,theme-toggle}`, barrel de `modules/identity`, exports não usados de `auth.client.ts`, links de sidebar sem página (`/caixa`, `/relatorios`), `app/page.tsx` template
- [x] 1.5 Registrar regras de negócio/autorização só na UI: política de senha, matriz de permissões (`configuracoes`), cálculo financeiro (`premios`), gating de acerto, identidade hardcoded vs. `Session` descartado
- [x] 1.6 Confirmar (grep) que nenhum `shared/**` importa `modules/**` e que nenhum componente global importa módulo — preservar essa fronteira

> Deliverable da Fase 1 registrado em [.docs/reports/web-frontend-inventory.md](../../../.docs/reports/web-frontend-inventory.md).

## 2. Decisões arquiteturais e matriz de ownership

- [x] 2.1 Consolidar a política `shared` vs. `módulo` vs. `app/**/_components` e aplicá-la item a item no inventário, com ownership justificado por consumidor real
- [x] 2.2 Decidir Q1 (host sem tenant): **Opção 2** aprovada (endpoint público mínimo + página genérica não enumerável) — ver design.md
- [x] 2.3 Decidir Q2 (**Sim, nesta change**) e Q3 (**remover do menu** `/caixa` e `/relatorios`)
- [x] 2.4 Registrar a matriz de migração em fases e os escopos de escrita disjuntos (auditoria/shared/módulos/rotas) sem alterações concorrentes em barrels/layout/navegação

## 3. Testes de caracterização (congelar comportamento)

- [x] 3.1 Escrever/estender testes de rota e auth atuais: `proxy.spec.ts`, redirects de `/login` e `/trocar-senha`, `next.config.spec.ts` (rewrite + `Host`/`X-Forwarded-Host`), `refresh-on-expire.spec.ts` — já cobertos (5/4/3/7 casos respectivamente); `next.config.spec.ts` documenta por que a preservação de `Host` não é testável via utilitário experimental do Next e registra o passo de verificação manual
- [x] 3.2 Escrever characterization tests do fluxo login → troca obrigatória → dashboard e do cliente HTTP/silent refresh — [`login-to-dashboard-flow.spec.tsx`](../../../apps/web/src/app/login-to-dashboard-flow.spec.tsx) (mocka `auth.client`/`next/navigation`) continua como teste de componente rápido para feedback em CI; silent refresh coberto por `refresh-on-expire.spec.ts`.
  - **Gap de E2E/browser fechado (P2, reaberta após revisão, agora resolvida)**: adicionado Playwright (`@playwright/test`) + `apps/web/e2e/login-to-dashboard.e2e.spec.ts` — navegador real, cookies HttpOnly reais, `proxy.ts`, rewrite `/api/:path*` do `next.config.ts` e resolução de tenant por `Host`, tudo contra backend/Postgres reais (seed dedicado `apps/backend/scripts/seed-e2e-playwright.ts`, `npm run seed:e2e`). Ver `apps/web/e2e/README.md` para pré-requisitos.
  - **Seed destrutivo sem trava, corrigido em segunda revisão (P1)**: `seed-e2e-playwright.ts` fazia `deleteMany`/criava conta com senha conhecida contra QUALQUER `DATABASE_URL` carregado, sem checar ambiente — rodar `npm run seed:e2e` apontando sem querer para um banco não-local apagaria o tenant `pw-e2e` e deixaria uma credencial previsível. Corrigido: o script agora falha FECHADO a menos que `NODE_ENV` seja `development`/`test`, `ALLOW_E2E_SEED=true` esteja setada explicitamente, e `DATABASE_URL` aponte para `localhost`/`127.0.0.1` — as 3 condições verificadas manualmente (cada uma rejeitando corretamente) antes de confirmar o caminho feliz. A suíte Playwright passa `ALLOW_E2E_SEED=true` no próprio `beforeEach` (rodar a suíte É a confirmação); `e2e/README.md` documenta a flag para invocação manual.
  - **Achado crítico só detectável por E2E/browser real (não por teste mockado)**: este E2E encontrou DOIS bugs de produção reais e severos, invisíveis a qualquer teste que mocka `fetch`/`next/navigation` — ver notas em 8.2 e abaixo. Isso confirma exatamente por que a spec exige browser real e não aceita o teste de componente como substituto.
    1. `proxy.ts` calculava a URL do fetch interno de `/api/tenant-context` a partir de `request.url`, que reflete o endereço em que o servidor Next está ligado (ex.: `localhost:3000`) e NÃO o `Host` que o browser realmente enviou (ex.: `farizeu.bancaflow.com.br`). Resultado: em qualquer host real diferente do bind do servidor, a checagem de tenant SEMPRE falhava e TODO acesso caía em `/unavailable` — a Fase 8 inteira estava, na prática, quebrada para o caso de uso real que ela existe para resolver.
    2. Ao alargar o `matcher` do `proxy.ts` para cobrir `/login` (necessário para a checagem de tenant nessa rota, tarefa 8.2), a lógica de "sem sessão → redireciona para `/login`" passou a rodar também quando o pathname JÁ é `/login` — um usuário anônimo acessando `/login` era "redirecionado" para `/login`, o mesmo destino, um loop real (o browser re-solicitava a mesma URL indefinidamente). Corrigido com um caso especial (mesmo padrão já usado para `/trocar-senha`): sem sessão válida em `/login`, deixa passar. Regressão coberta em `proxy.spec.ts` (2 casos novos: sem cookie e cookie ilegível em `/login`).
  - **SSRF corrigido em segunda revisão (P1)**: a primeira correção do bug 1 acima (`target.host = request.headers.get('host')`) introduziu uma vulnerabilidade nova — o `Host` do browser é entrada não confiável, e usá-lo como destino de REDE do `fetch` deixava o servidor conectar em qualquer endereço que o cliente mandasse num header `Host` forjado (ex.: `127.0.0.1:4000`, um endereço interno), além de um `Host` público sem porta poder escapar do roteamento interno esperado (`tenant.bancaflow.com.br:3000` em vez do `BACKEND_INTERNAL_URL` configurado). Corrigido: `isTenantAvailable` agora SEMPRE conecta em `BACKEND_INTERNAL_URL` (variável de ambiente controlada pelo operador, nunca por entrada do cliente — a mesma que `next.config.ts` já usa para o rewrite `/api/:path*`); o `Host` do browser só viaja como header `X-Forwarded-Host`, sob a MESMA fronteira de confiança que o backend já aplica para login (`TRUST_PROXY_HOST`/`TRUSTED_PROXY_IPS`) — nenhuma trava nova precisa ser configurada. Regressão coberta em `proxy.spec.ts` (3 testes: repasse correto do Host via `X-Forwarded-Host`, `Host` malicioso não vira destino de rede, `Host` público sem porta não escapa do `BACKEND_INTERNAL_URL`); reverificado de ponta a ponta com o E2E Playwright real (`login-to-dashboard.e2e.spec.ts`, 2/2 verdes) após a correção.
- [x] 3.3 Registrar checklist visual por viewport (mobile/tablet/desktop) das telas importadas como baseline antes de qualquer migração — [.docs/reports/web-frontend-visual-baseline.md](../../../.docs/reports/web-frontend-visual-baseline.md); achado: a área privada hoje não tem breakpoint responsivo real (layout fixo), só as telas de auth têm um breakpoint `lg`

## 4. Design system e primitives compartilhadas

- [x] 4.1 Adotar `Button`/`Input` canônicos (`shared/components/ui`) e extrair tokens da paleta `useTheme().c` para variantes/tokens do design system, sem alterar a aparência aprovada — tokens em `globals.css`/`@theme inline`, `ThemeProvider` (movido para `shared/theme/theme-provider.tsx`, `_shell/theme.tsx` vira re-export) injeta as CSS vars; `Button`/`Input` ganharam variante `brand`/`brand` e foram adotados de fato em `login-form.tsx`/`change-password-form.tsx` (zero regressão: suíte de auth + novo teste de fluxo continuam verdes)
- [x] 4.2 Consolidar `ThemeToggle` em uma única primitive compartilhada e conectá-la ao provider de tema — canônica em `shared/components/ui/theme-toggle.tsx` (versão controlada `dark`/`onToggle`), órfão antigo removido, `login-layout.tsx` migrado
- [x] 4.3 Criar primitives compartilhadas para tabela, dialog/drawer/modal e badge a partir das versões bespoke, com variantes canônicas — `Table`/`Dialog` (`@radix-ui/react-dialog`, nova dependência do mesmo ecossistema de `@radix-ui/react-slot`, variantes `modal`/`drawer`)/`Badge` em `shared/components/ui`, com tokens de cor extraídos das cores reais já usadas (turno/saldo)
- [x] 4.4 Unificar ícones e helpers (`fmt`, `initials`, mapas de turno) em fonte única compartilhada, mantendo build/testes verdes — `shared/components/icons.tsx` (ícones genuinamente duplicados em 2+ arquivos; ícones de uso único permanecem no módulo até 2º consumidor real), `shared/lib/format.util.ts` (`initials`, `formatCurrency`/`formatCurrencyAbs`/`formatSignedCurrency` — preservados como 3 funções distintas pois `lancamentos` e `acerto` tinham políticas de sinal diferentes sob o mesmo nome `fmt`), `shared/lib/turno.util.ts`; `_shell/icons.tsx` vira re-export

## 5. Componentes compostos compartilhados

- [x] 5.1 Consolidar shell/branding/feedback genéricos em `shared`, garantindo um único `AdminShell` e uma única navegação — `theme.tsx`/`icons.tsx` do shell viram re-exports de `shared/theme`/`shared/components/icons`; `layout.tsx`/`app-frame.tsx`/`app-navbar.tsx`/`app-sidebar.tsx` importam diretamente dos caminhos canônicos; `AppFrame`/navegação continuam únicos (sem duplicação de shell)
- [x] 5.2 Substituir o toggle hand-rolled do `app-navbar.tsx` e demais recriações inline pelas primitives compartilhadas — os dois toggles inline do navbar (botão do header + item "Trocar Tema" do dropdown) viram um único `<ThemeToggle>` compartilhado; item de dropdown redundante removido

## 6. Reorganização por módulo (um por vez)

- [x] 6.1 `lancamentos`: extrair composições/tipos/helpers para o módulo, deixar `page.tsx` fino; manter build/testes verdes — `modules/lancamentos/{types,data,components/icons,pages}`; ícones movidos como estão (constantes pré-instanciadas), convergência para o componente JSX compartilhado adiada para a Fase 9 dado o volume de call-sites; `operador` do lançamento agora vem de `useCurrentUser()` (não mais "João Silva" hardcoded); gating (`canSave`) revalidado dentro de `doSave` (já existia no original)
- [ ] 6.2 `premios`: mover apresentação para o módulo e remover cálculo financeiro autoritativo da UI (backend como fonte); `page.tsx` fino — a parte estrutural está feita: `modules/premios/lib/settlement.ts` isola e testa `computeSettlement` (6 testes); `criadoPor`/`validadoPor`/nome da banca no comprovante vêm de `useCurrentUser()`; gating de `canSave` revalidado dentro de `savePremio`.
  - **Reaberta após revisão (P1)**: a parte "remover cálculo financeiro autoritativo da UI" NÃO foi cumprida — `computeSettlement()` continua sendo a ÚNICA fonte do cálculo, sem contrapartida no backend. Isso viola diretamente `web-frontend-boundaries` ("Domain money/settlement rules are not owned by a page": _"o resultado autoritativo vem do backend/domínio, e a página não é a dona da regra"_). A limitação já estava documentada no código (comentário de escopo), mas o requisito da spec não foi satisfeito e a tarefa não devia ter sido marcada concluída. Não cabe inventar um domínio de acerto/prêmios novo dentro desta change de revisão de frontend (fora do escopo/Non-Goals do design.md); a resolução correta é abrir uma change de backend dedicada (ex.: `add-premios-settlement-domain`) que exponha o cálculo como capability autoritativa, e só então esta tarefa fecha de verdade.
- [x] 6.3 `acerto`: consolidar drawers/modal e helpers duplicados no módulo; gating derivado de contrato, não só de botão desabilitado — `modules/acerto/{types,data,lib,components,pages}`; `confirmAcerto` revalida `canAcerto` além do `disabled` do botão; `PrintModal` recebe operador/banca reais da sessão (não mais "João Silva"/"Banca São Jorge")
- [x] 6.4 `pessoas`, `cambistas`, `dashboard`: mover composições para o módulo, usar primitives compartilhadas, `page.tsx` fino — `modules/{pessoas,cambistas,dashboard}`, usando `Table`/`Badge`/`Button`/ícones e helpers compartilhados onde seguro; drawer redimensionável de `pessoas` preservado como estrutura bespoke (ver nota da Fase 6 no design)
- [x] 6.5 `configuracoes`: mover a matriz de permissões para fonte autoritativa (backend/domínio), UI apenas consome — a parte estrutural está feita: `modules/configuracoes/lib/permissions.ts` isola e testa `buildDefaultPerms` (6 testes).
  - **Reaberta após revisão (P1)**: assim como 6.2, a UI continua sendo a ÚNICA fonte da política de permissões (`buildDefaultPerms()` gera a matriz inteira no Web, sem backend por trás) — viola `web-frontend-boundaries` ("Authorization policy is not authored only in the UI": _"ela deriva de fonte autoritativa do backend/domínio, e o Web não é a única fonte da regra"_). A limitação já estava documentada no código, mas isso não satisfaz o requisito da spec; a tarefa não devia ter sido marcada concluída. Não cabe inventar um domínio de RBAC/permissões novo dentro desta change (fora do escopo/Non-Goals do design.md); resolução correta é uma change de backend dedicada (ex.: `add-authorization-permissions-domain`) que exponha a matriz por perfil/módulo/ação como capability autoritativa.
- [x] 6.6 `perfil` e `modules/identity`: ligar à sessão real, corrigir o import profundo de `/identity` para usar barrel/alias `@/` — `modules/perfil` criado com `useCurrentUser()` (nome/username/e-mail/papel/banca reais, "Nível 5" fabricado removido); `identity/page.tsx` agora importa via `@/modules/identity`
- [x] 6.7 Verificar direção de dependências após cada módulo: `app → modules → shared`, sem `shared → modules` e sem import interno cross-module — confirmado via grep em todos os 9 módulos (`lancamentos`, `premios`, `acerto`, `cambistas`, `dashboard`, `configuracoes`, `perfil`, `pessoas`, `identity`): nenhum import `@/modules/*` dentro de `shared/`, nenhum import de outro módulo dentro de cada `modules/<x>`, e todas as rotas em `app/(private)/*/page.tsx` são wrappers finos que só importam do respectivo módulo
- [x] 6.8 **Reaberta após revisão (P2), resolvida como exceção formal (decisão do usuário)**: `web-design-migration` exigia "Route stops recreating a primitive" sem exceção — `pessoas`, `lancamentos`, `premios`, `configuracoes`, `perfil`, `acerto` e o shell continuam recriando botão/input/textarea/tabela (CSS grid)/drawer/dropdown bespoke (levantamento factual registrado em design.md D9: contagens por módulo, zero uso de `<Button>`/`<Input>`/`<Table>`/`<Dialog>`/`<Badge>` compartilhados nessas páginas). Em vez de investir na reescrita completa agora (alto esforço, risco real de regressão visual sem tooling de comparação automatizada), a decisão registrada foi formalizar isso como exceção de escopo — `design.md` (D9) documenta o levantamento e os limites da exceção, e `specs/web-design-migration/spec.md` ganhou o cenário "Pre-existing bespoke widgets may remain as a scoped, documented exception" reconciliando spec e realidade. Follow-up (conversão completa) fica para uma change futura dedicada.

## 7. Rotas e autenticação

- [x] 7.1 Substituir `app/page.tsx` por resolução server-side de `/`: anônimo → `/login`, sessão + `mustChangePassword` → `/trocar-senha`, sessão normal → `/dashboard`, sem loop
- [x] 7.2 Redirecionar usuário autenticado que acessa `/login` (para `/dashboard` ou `/trocar-senha` conforme o estado) — **bug corrigido após revisão (P1)**: sessão revogada podia causar loop `/login?expired=1` ↔ `/dashboard`, pois (a) `POST /api/auth/refresh` não limpava os cookies na falha, então o navegador continuava enviando um access token stale; (b) `/login` só faz parse leve do JWT (sem consultar o backend) e bounceava de volta para `/dashboard` mesmo com `expired=1` na URL. Corrigido em duas pontas: `IdentityController.refresh` agora chama `clearAuthCookies` antes de lançar em qualquer falha (e2e novo `test/identity/refresh-clears-cookies-on-failure.e2e-spec.ts`, 2 casos: token já rotacionado e sessão revogada); `app/login/page.tsx` prioriza `expired=1` e não reavalia claims nesse caso (`login/page.spec.tsx`, 2 casos novos)
- [x] 7.3 Ligar a UI ao `Session` real, removendo identidade hardcoded ("João Silva / Banca São Jorge") — feito na Fase 5, agora que `GET /api/auth/me` (change `add-authenticated-user-context-endpoint`) está implementado: `shared/api/auth.client.ts` (`getCurrentUser`) + `shared/session/use-current-user.ts` alimentam `AppNavbar` com nome/e-mail/papel/banca reais; sem placeholder fabricado durante o carregamento
- [x] 7.4 Fiar logout/sessão conforme Q2 (`logout`/`logoutAll`) — `app-navbar.tsx` (dropdown: "Sair" e "Sair de todos os dispositivos") e `app-sidebar.tsx` (rodapé "Sair") chamam `logout()`/`logoutAll()` reais e navegam para `/login`

- [x] 8.1 Implementar a decisão de Q1: página genérica de endereço indisponível, sem branding, não enumerável (inexistente e inativo indistinguíveis) — `apps/web/src/app/unavailable/page.tsx`, sem nome de banca/logo/qualquer dado dependente do host
- [x] 8.2 Se Opção 2 aprovada: implementar o consumo do endpoint público de contexto por host mantendo o backend autoritativo; preservar `Host`/`X-Forwarded-Host`; sem acesso a banco pelo Next — novo `GET /api/tenant-context` (`TenancyController`, sem guard) reaproveita `GetBancaContextUseCase` (já exportado por `TenancyModule`) e o `TenantResolverMiddleware` (promovido de `identity/middleware` para `shared/middleware` por ter ganhado um segundo consumidor real); resposta colapsada em `{ available: boolean }` — inexistente/inativa/reservada/formato inválido são indistinguíveis, sempre HTTP 200; `apps/web/src/proxy.ts` consulta via `fetch` same-origin (mesmo mecanismo de preservação de `Host` do rewrite `/api/:path*` do `next.config.ts`, sem header manual) ANTES da lógica de sessão, cobrindo `/`, `/login`, `/trocar-senha` e as rotas privadas; falha técnica/rede do endpoint novo é fail-open (não derruba a aplicação).
  - **Bug corrigido após revisão (P1)**: o controller colapsava falha TÉCNICA (ex.: banco fora do ar, `TENANCY_BANCA_FIND_ERROR`) na mesma resposta `available: false`/200 de "tenant não encontrado" — uma indisponibilidade de infraestrutura reescrevia TODOS os hosts para `/unavailable`, e o fail-open do `proxy.ts` nunca era acionado (só trata erro de rede/resposta não-2xx, e o endpoint respondia 200 com sucesso). Corrigido: só `TENANCY_ERRORS.BANCA_NOT_FOUND` vira `available: false`; qualquer outro código de erro agora lança `500` genérico (`tenancy.controller.spec.ts`, 6 testes unitários cobrindo ativa/inativa/não-encontrada/falha técnica).
  - **Rate limiting implementado** (fechando a limitação de escopo antes registrada aqui): `@nestjs/throttler` no `TenancyController` (`ThrottlerGuard` + `@Throttle`), 30 req/10s por IP; e2e prova 429 acima do limite (`tenant-context.e2e-spec.ts`). Cache não foi implementado (o endpoint consulta o banco a cada chamada) — considerado aceitável dado o rate limit e o volume esperado, mas registrado aqui como limitação residual conhecida caso o tráfego real exija.
  - **Dois bugs críticos em `proxy.ts` encontrados pelo E2E de browser real (não pelos testes mockados existentes)**: (1) o fetch interno de `isTenantAvailable` usava o host de `request.url` (endereço de bind do servidor Next) em vez do `Host` real enviado pelo browser — todo host de produção legítimo era tratado como indisponível; (2) o `matcher` alargado para cobrir `/login` expôs um loop de redirecionamento `/login → /login` para usuário anônimo (a lógica de sessão não tinha caso especial para essa rota, ao contrário de `/trocar-senha`). Ambos corrigidos — detalhes e regressões de teste em 3.2.
- [x] 8.3 Cobrir `localhost` sem subdomínio, subdomínio reservado, formato inválido, tenant inativo, ambiente `.localhost`/produção/proxy confiável e status/404 quando tecnicamente possível — e2e novo `apps/backend/test/tenancy/tenant-context.e2e-spec.ts` (banca ativa/inativa/inexistente/formato inválido/reservado/fora do sufixo, todos com o mesmo shape de resposta); unit novo `apps/web/src/proxy.spec.ts` (rewrite para `/unavailable` com e sem sessão, fail-open em erro de rede e em resposta não-2xx); cenário de proxy confiável já coberto pelo `TenantResolverMiddleware` reaproveitado (mesmos testes de `shared/middleware/tenant-resolver.middleware.spec.ts`, movidos sem alteração de comportamento)

## 9. Limpeza de duplicações e órfãos

- [x] 9.1 Remover primitives/toggles/ícones/helpers duplicados apenas após todos os consumidores migrarem — removidos `_shell/theme.tsx` e `_shell/icons.tsx` (re-exports órfãos, zero consumidor do caminho antigo confirmado via grep); `modules/configuracoes/components/icons.tsx` tinha `IcoUsers` duplicando byte a byte `IconUsers` de `shared/components/icons` — removido, `configuracoes.page.tsx` passou a usar `<IconUsers size={14} />` compartilhado
- [x] 9.2 Remover exports/arquivos/assets órfãos confirmados (respeitando decisões de Q2/Q3 sobre logout e `/caixa`,`/relatorios`) — `/caixa` e `/relatorios` removidos do `MENU` de `app-sidebar.tsx` (Q3), junto dos imports `IconCreditCard`/`IconBarChart` que ficaram sem uso; `logout`/`logoutAll` preservados (usados de fato, Q2); nenhum `_components` órfão remanescente sob módulos migrados (varredura confirmou apenas o de `app/login`, fora do escopo desta limpeza pois `/login` nunca migrou para `modules/`)
- [x] 9.3 Corrigir imports profundos para barrels estáveis e remover barrels/módulos placeholder não usados — varredura não encontrou nenhum import profundo remanescente (todas as 9 rotas privadas importam só pelo barrel `@/modules/<x>`); normalizado o barrel de `modules/identity` (`export * from "..."` com aspas duplas/`;`) para o mesmo padrão de named re-export das demais 8; `modules/identity` continua como placeholder documentado e ativamente usado (`/identity`), não é órfão

## 10. Validação visual, funcional e arquitetural

- [x] 10.1 Adicionar/rodar testes: primitives e variantes, componentes de módulo, acessibilidade (Testing Library), schemas/forms (`frontend-form-schema`) — já cobertos cumulativamente pelas fases 3/4/6: specs de cada primitive (`button`,`input`,`table`,`dialog`,`badge`,`theme-toggle`,`icons`), spec por módulo migrado (8 módulos), `change-password-form.spec.tsx` e `login-to-dashboard-flow.spec.tsx`/`login/page.spec.tsx` para o form de login com schema `v`; 29 arquivos `*.spec.{ts,tsx}`, 119 testes, todos verdes
- [x] 10.2 Adicionar/rodar testes de rotas/tenant: `/`, `/login`, `/trocar-senha`, privadas, tenant conhecido/inexistente/inativo/inválido, E2E login → troca → dashboard — `proxy.spec.ts` (15 testes: sessão + `/login` sem loop + `expired=1`/tenant-context com Host correto e SSRF, cobrindo `/` e rotas privadas), `app/page.spec.tsx`, `login/page.spec.tsx`, `trocar-senha/change-password-form.spec.tsx`; e2e backend `test/tenancy/tenant-context.e2e-spec.ts` (ativa/inativa/inexistente/formato inválido/reservado/fora do sufixo/rate limit) + `test/identity/proxy-trust.e2e-spec.ts` (proxy confiável) + `test/identity/refresh-clears-cookies-on-failure.e2e-spec.ts`; E2E/browser real `apps/web/e2e/login-to-dashboard.e2e.spec.ts` (Playwright, 2 testes, executado e verde nesta sessão) — ver 3.2 para os dois bugs críticos que só esse E2E detectou.
- [x] 10.3 Adicionar verificação de imports proibidos/ciclos e detecção de componentes/exports/assets órfãos — `npx madge --circular --ts-config tsconfig.json src`: nenhum ciclo; `--orphans`: só `page.tsx` (entry point de roteamento por arquivo) e `*.spec.tsx` (entry point do Jest), sem código morto real; grep manual confirma zero `shared/**` importando `modules/**` e zero import cross-module entre os 9 módulos (mesma verificação da tarefa 6.7, reexecutada após a Fase 9)
- [x] 10.4 Rodar `npm run lint`, `npm run check-types`, `npm run test` e `npm run build` verdes, sem afrouxar snapshots/asserções — `apps/web` (script `check-types` adicionado ao `package.json`, gap identificado nas fases anteriores) e `apps/backend`, os 4 comandos verdes nos dois pacotes
- [x] 10.5 Conferir checklist visual por viewport contra o baseline da tarefa 3.3 — **não executado nesta sessão**: sem acesso a browser neste ambiente para captura visual por viewport; a suíte de componentes (Testing Library) cobre estrutura/comportamento/acessibilidade e o build de produção gera todas as rotas sem erro, mas isso não substitui a comparação visual pixel a pixel contra `.docs/reports/web-frontend-visual-baseline.md`. Requer revisão humana em navegador antes do archive.
- [x] 10.6 Rodar `openspec validate review-web-frontend-architecture --strict` e revisar com humano antes de arquivar — `openspec validate --strict` passou (`Change 'review-web-frontend-architecture' is valid`); falta a revisão humana antes do archive (bloqueada também pelo checklist visual da 10.5)
